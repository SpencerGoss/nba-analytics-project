---
phase: 04-rest-schedule-features
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/features/team_game_features.py
  - src/models/game_outcome_model.py
autonomous: true
requirements: [FR-3.4, NFR-1, NFR-2]

must_haves:
  truths:
    - "season_month integer 1-12 is present for every game in game_matchup_features.csv"
    - "season_month is computed from game_date directly on the matchup DataFrame (not per-team)"
    - "get_feature_cols() schedule_cols includes home_travel_miles, away_travel_miles, home_cross_country_travel, away_cross_country_travel, and season_month"
    - "Model retrains successfully with the new schedule features included"
    - "Pipeline completes in under 15 minutes total (NFR-2)"
  artifacts:
    - path: "src/features/team_game_features.py"
      provides: "season_month computation in build_matchup_dataset()"
      contains: "season_month"
    - path: "src/models/game_outcome_model.py"
      provides: "Updated schedule_cols with travel and season_month features"
      contains: "home_travel_miles"
    - path: "data/features/game_matchup_features.csv"
      provides: "Rebuilt matchup features with season_month column"
    - path: "models/artifacts/game_outcome_model.pkl"
      provides: "Retrained model including Phase 4 features"
  key_links:
    - from: "build_matchup_dataset() in team_game_features.py"
      to: "season_month column in game_matchup_features.csv"
      via: "pd.to_datetime(matchup['game_date']).dt.month"
      pattern: "season_month.*dt\\.month"
    - from: "get_feature_cols() in game_outcome_model.py"
      to: "model training feature selection"
      via: "schedule_cols set membership"
      pattern: "schedule_cols"
---

<objective>
Add season_month as a game-level feature in the matchup dataset, wire all Phase 4 schedule features into the model's feature selection, and retrain the game outcome model.

Purpose: Complete Phase 4 by adding the final feature (season-segment context), ensuring the model can select all new schedule features during training, and producing a new model artifact that includes travel, timezone, and season-month signals.

Output: Updated build_matchup_dataset() with season_month computation. Updated get_feature_cols() with full Phase 4 schedule_cols. Rebuilt matchup CSV. Retrained model artifact.
</objective>

<execution_context>
@C:/Users/spenc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spenc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rest-schedule-features/04-RESEARCH.md
@.planning/phases/04-rest-schedule-features/04-01-SUMMARY.md
@src/features/team_game_features.py
@src/models/game_outcome_model.py

<interfaces>
<!-- From team_game_features.py build_matchup_dataset() -->
<!-- season_month insertion point: after the matchup merge, before diff_four_factors_composite -->

```python
matchup = (
    meta
    .merge(home_feat, on="game_id", how="inner")
    .merge(away_feat, on="game_id", how="inner")
)
# INSERT season_month HERE
# ...
matchup["diff_four_factors_composite"] = _four_factors_composite(...)
```

<!-- From game_outcome_model.py get_feature_cols() -->
```python
schedule_cols = {
    "home_days_rest", "away_days_rest",
    "home_is_back_to_back", "away_is_back_to_back",
}
```
<!-- After 04-01, matchup will have: home_travel_miles, away_travel_miles, diff_travel_miles,
     home_cross_country_travel, away_cross_country_travel, diff_cross_country_travel,
     diff_is_back_to_back. schedule_cols needs updating to include these + season_month. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add season_month to build_matchup_dataset and wire schedule_cols in get_feature_cols</name>
  <files>src/features/team_game_features.py, src/models/game_outcome_model.py</files>
  <action>
1. In `build_matchup_dataset()` in team_game_features.py, add season_month computation AFTER the matchup merge (after `matchup = meta.merge(home_feat...).merge(away_feat...)`) and BEFORE the differential features section:

```python
    # -- Season-segment context (Phase 4, FR-3.4) --
    matchup["season_month"] = pd.to_datetime(matchup["game_date"]).dt.month
    print(f"  season_month: {matchup['season_month'].nunique()} unique months, "
          f"range {int(matchup['season_month'].min())}-{int(matchup['season_month'].max())}")
```

This is a game-level feature (same for both teams) so it goes directly on the matchup DataFrame, NOT in per-team features. Do NOT add it to team_game_features.py build_team_game_features(). See Pitfall 6 in research.

2. In `game_outcome_model.py`, update `get_feature_cols()` schedule_cols to include all Phase 4 features:

```python
    schedule_cols = {
        "home_days_rest", "away_days_rest",
        "home_is_back_to_back", "away_is_back_to_back",
        "home_travel_miles", "away_travel_miles",
        "home_cross_country_travel", "away_cross_country_travel",
        "season_month",
    }
```

Note: season_month is game-level (not prefixed home_/away_). The diff_ variants of travel_miles, cross_country_travel, and is_back_to_back are automatically picked up by the `c.startswith("diff_")` filter -- they do NOT need to be in schedule_cols.

3. Run `python src/features/team_game_features.py` to rebuild both CSVs with season_month.

4. Verify season_month is present with correct range:
   - `python -c "import pandas as pd; df = pd.read_csv('data/features/game_matchup_features.csv'); print(df['season_month'].value_counts().sort_index())"`
   - Expected: months 1-12 (Oct=10 through Apr=4, possibly May=5 for late season)

IMPORTANT: All print statements must use ASCII only. No Unicode characters.
  </action>
  <verify>
    <automated>python -c "
import pandas as pd
df = pd.read_csv('data/features/game_matchup_features.csv')
assert 'season_month' in df.columns, 'season_month missing'
assert df['season_month'].between(1, 12).all(), 'season_month out of range'
print('season_month distribution:')
print(df['season_month'].value_counts().sort_index())
# Verify all Phase 4 features present
required = [
    'home_travel_miles', 'away_travel_miles', 'diff_travel_miles',
    'home_cross_country_travel', 'away_cross_country_travel', 'diff_cross_country_travel',
    'diff_is_back_to_back', 'season_month',
]
missing = [c for c in required if c not in df.columns]
assert not missing, f'Missing: {missing}'
print('All Phase 4 columns verified in matchup features')
"</automated>
  </verify>
  <done>season_month (1-12) is computed from game_date in build_matchup_dataset(). get_feature_cols() schedule_cols includes home_travel_miles, away_travel_miles, home_cross_country_travel, away_cross_country_travel, and season_month. game_matchup_features.csv contains all Phase 4 columns.</done>
</task>

<task type="auto">
  <name>Task 2: Retrain game outcome model with Phase 4 schedule features</name>
  <files>src/models/game_outcome_model.py</files>
  <action>
1. Run model training: `python src/models/game_outcome_model.py`

2. The model will automatically pick up the new schedule features because:
   - diff_travel_miles, diff_cross_country_travel, diff_is_back_to_back are captured by the `c.startswith("diff_")` filter
   - home_travel_miles, away_travel_miles, home_cross_country_travel, away_cross_country_travel, season_month are captured by schedule_cols membership

3. Verify the training output:
   - Check that feature count increased (should be higher than pre-Phase-4)
   - Check that test accuracy is reported (should be >= 0.64 approximately)
   - Check that some Phase 4 features appear in the top 15 importances (or at least that the model trained without error)

4. Verify model artifact was saved:
   - `models/artifacts/game_outcome_model.pkl` should have a recent timestamp
   - `models/artifacts/game_outcome_metadata.json` should show the updated feature list

5. Check total pipeline time: run `python -c "import time; t0=time.time(); exec(open('src/features/team_game_features.py').read()); print(f'Feature build time: {time.time()-t0:.0f}s')"` to verify NFR-2 (under 15 min). The travel computation adds less than 1 second to the pipeline per research benchmarks.

Note: If calibration.py exists and was previously run, the calibrated model may need regeneration. Print a note if `game_outcome_model_calibrated.pkl` exists and is older than the newly trained model. Do NOT run calibration automatically -- just note it.
  </action>
  <verify>
    <automated>python -c "
import os, json
meta_path = 'models/artifacts/game_outcome_metadata.json'
assert os.path.exists(meta_path), 'Model metadata not found'
with open(meta_path) as f:
    meta = json.load(f)
feats = meta.get('feature_list', [])
phase4 = [f for f in feats if 'travel' in f or 'cross_country' in f or 'season_month' in f or 'back_to_back' in f]
print(f'Total features: {len(feats)}')
print(f'Phase 4 features in model: {phase4}')
assert len(phase4) >= 5, f'Expected >= 5 Phase 4 features, got {len(phase4)}: {phase4}'
print(f'Test accuracy: {meta.get(\"test_accuracy\", \"N/A\")}')
print('Model retrained successfully with Phase 4 features')
"</automated>
  </verify>
  <done>Game outcome model retrained with all Phase 4 schedule features. Model artifact and metadata saved. Feature list in metadata includes travel_miles, cross_country_travel, is_back_to_back, and season_month variants. Pipeline runtime remains under 15 minutes (NFR-2).</done>
</task>

</tasks>

<verification>
1. game_matchup_features.csv contains season_month with values 1-12
2. game_outcome_model.py get_feature_cols() schedule_cols includes all Phase 4 columns
3. Model metadata JSON shows Phase 4 features in the feature list
4. Model trains without error and reports test accuracy
5. All Phase 4 features (FR-3.1 through FR-3.4) are reflected in the training data
6. Total pipeline runtime stays under 15 minutes (NFR-2)
</verification>

<success_criteria>
- `python -c "import json; meta=json.load(open('models/artifacts/game_outcome_metadata.json')); phase4=[f for f in meta['feature_list'] if 'travel' in f or 'cross_country' in f or 'season_month' in f or 'back_to_back' in f]; assert len(phase4)>=5; print(f'{len(phase4)} Phase 4 features in model')"` passes
- `python -c "import pandas as pd; df=pd.read_csv('data/features/game_matchup_features.csv'); assert 'season_month' in df.columns and df['season_month'].between(1,12).all(); print('season_month verified')"` passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-rest-schedule-features/04-02-SUMMARY.md`
</output>
