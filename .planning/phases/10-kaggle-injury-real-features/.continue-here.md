---
phase: 10-kaggle-injury-real-features
task: 0
total_tasks: 4
status: not_started
last_updated: "2026-03-05T05:51:37Z"
closes: [DATA-01, FEAT-01]
---

<current_state>
Phase 10 created as gap-closure phase from v2.0 milestone audit. Not yet started.
This phase must download the Kaggle NBA injury dataset and wire real absence features
into the training pipeline to satisfy DATA-01 and FEAT-01.
</current_state>

<completed_work>
None — phase not yet started.
</completed_work>

<remaining_work>
- Task 1: Download Kaggle NBA injury dataset via `kaggle datasets download`
  - Dataset: likely "wyattowalsh/basketball" or search for NBA injury 2016-2025
  - Save to data/raw/injuries/ as structured CSV
  - Schema needed: player_id, game_date, absence_flag, injury_type
- Task 2: Build src/features/injury_absence_features.py
  - Read Kaggle CSVs, normalize player_id/team/date to project format
  - Produce per-team-game absence aggregates (missing_minutes, star_player_out, n_missing_rotation)
  - Must use shift(1) before any rolling — no data leakage
  - Replace the call to build_injury_proxy_features() in team_game_features.py
- Task 3: Regenerate game_matchup_features.csv
  - Run python src/features/team_game_features.py
  - Verify injury columns are now sourced from real data (non-zero variance, non-trivial)
- Task 4: Retrain models with new features
  - python src/models/game_outcome_model.py
  - python src/models/calibration.py
  - python src/models/ats_model.py
  - python src/models/backtesting.py && python src/models/ats_backtest.py
  - Run pytest -q (59 tests must pass)
  - Commit
</remaining_work>

<decisions_made>
- Kaggle CLI 2.0.0 is installed and available (confirmed this session)
- Keep injury_proxy.py as fallback for pre-Kaggle seasons (before 2016)
- shift(1) rule is mandatory for all new absence features
- Training path only — injury absence features are for model training, NOT inference path (FR-4.4 boundary)
</decisions_made>

<blockers>
- Need to find the correct Kaggle dataset slug for NBA injuries 2016-2025
  - Try: kaggle datasets search "nba injury" to find the right one
  - Candidate: "wyattowalsh/basketball" has injury data
- Kaggle API key must be configured in ~/.kaggle/kaggle.json (likely already set since CLI works)
</blockers>

<context>
This is the biggest remaining gap in v2.0. The original plan called for Kaggle injury data
but Phase 6 only built an inference-path daily PDF fetcher. The training pipeline still
uses a rolling game-log proxy as injury signal. Phase 10 closes this by downloading the
actual historical dataset and wiring it into the feature engineering pipeline.

After this phase, Phase 11 (ensemble + metadata cleanup) is small and quick.
Then re-run gsd:audit-milestone to confirm all 14 requirements satisfied, then
gsd:complete-milestone to archive v2.0 and plan v3.0.
</context>

<next_action>
Run: kaggle datasets search "nba injury" to find correct dataset slug
Then: kaggle datasets download <slug> -p data/raw/injuries/ --unzip
Then implement src/features/injury_absence_features.py
</next_action>
