---
phase: 05-ats-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/get_historical_odds.py
  - src/features/ats_features.py
  - data/features/game_ats_features.csv
autonomous: true
requirements: [FR-5.1, FR-5.4, NFR-1]

must_haves:
  truths:
    - "game_ats_features.csv exists as a separate file from game_matchup_features.csv"
    - "game_ats_features.csv contains spread, home_implied_prob, away_implied_prob, and covers_spread columns"
    - "game_matchup_features.csv does NOT contain spread, implied probability, or covers_spread columns"
    - "Historical odds from Kaggle dataset are downloaded and persisted in data/raw/odds/"
    - "Team name mapping between Kaggle format and project 3-letter abbreviations produces >0 joined rows"
    - "Pushes (id_spread=2) are excluded from covers_spread target (stored as NaN)"
  artifacts:
    - path: "src/data/get_historical_odds.py"
      provides: "Download/load Kaggle betting CSV, normalize team names, persist to data/raw/odds/"
    - path: "src/features/ats_features.py"
      provides: "build_ats_features() joining matchup features with betting odds"
      exports: ["build_ats_features"]
    - path: "data/features/game_ats_features.csv"
      provides: "ATS feature table with spread + implied prob + covers_spread target"
  key_links:
    - from: "src/features/ats_features.py"
      to: "data/features/game_matchup_features.csv"
      via: "pd.read_csv inner join with odds data"
      pattern: "matchup\\.merge.*odds"
    - from: "src/features/ats_features.py"
      to: "data/raw/odds/nba_betting_historical.csv"
      via: "load_and_normalize_odds()"
      pattern: "load_and_normalize_odds"
---

<objective>
Build the ATS feature table by downloading historical odds data and joining it with existing matchup features.

Purpose: Create the separate `game_ats_features.csv` that extends matchup features WITH Vegas spread and implied moneyline probability -- the foundation for ATS classifier training. This strict data separation ensures the win-probability model never sees spread data.

Output: `src/data/get_historical_odds.py`, `src/features/ats_features.py`, `data/features/game_ats_features.csv`
</objective>

<execution_context>
@C:/Users/spenc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spenc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ats-model/05-RESEARCH.md

<interfaces>
<!-- Key interfaces the executor needs -->

From scripts/fetch_odds.py:
```python
ODDS_TEAM_TO_ABB = {
    "Atlanta Hawks": "ATL", "Boston Celtics": "BOS", ...  # 30 team mapping
}
WINPROB_FLAG_PP = 0.05  # 5 percentage points
def american_odds_to_implied_prob(ml: int | None) -> float | None:
    """Convert American moneyline odds to implied probability (vig-inclusive)."""
```

From src/models/game_outcome_model.py:
```python
MATCHUP_PATH = "data/features/game_matchup_features.csv"
EXCLUDED_SEASONS = ["201920", "202021"]
MODERN_ERA_START = "201314"
def get_feature_cols(df: pd.DataFrame) -> list:
    """Prefer compact matchup differential features with critical context."""
```

From data/features/game_matchup_features.csv:
- 272 columns, 15,304 modern-era rows
- Key join columns: game_date, home_team, away_team, season
- NO spread or implied probability columns (by design)

Kaggle NBA Betting Dataset (nba-betting-data-october-2007-to-june-2024):
- Columns: season, date, away, home, score_away, score_home, whos_favored, spread, total, moneyline_away, moneyline_home, id_spread, id_total
- id_spread: 1=favorite covered, 0=underdog covered, 2=push
- Moneyline absent from ESPN-sourced rows (Jan 2023+)
- Coverage: October 30, 2007 through June 2025
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download Kaggle historical odds and build normalization module</name>
  <files>src/data/get_historical_odds.py</files>
  <action>
Create `src/data/get_historical_odds.py` that:

1. Downloads the Kaggle NBA betting dataset CSV. Since Kaggle downloads require authentication, implement a two-path approach:
   - **Primary:** Check if `data/raw/odds/nba_betting_historical.csv` already exists locally. If so, load it.
   - **Fallback:** If not present, print clear instructions for the user to download from `https://www.kaggle.com/datasets/cviaxmiwnptr/nba-betting-data-october-2007-to-june-2024` and place the CSV in `data/raw/odds/`.
   - Create `data/raw/odds/` directory if it doesn't exist.

2. Build `KAGGLE_TEAM_TO_ABB` mapping dict by inspecting the unique team name format in the Kaggle CSV. The Kaggle dataset likely uses condensed names like "GoldenState", "LAClippers". Map all 30 teams to the project's 3-letter abbreviations (reuse knowledge from `ODDS_TEAM_TO_ABB` in `scripts/fetch_odds.py` for the abbreviation side).

3. Implement `load_and_normalize_odds(odds_path: str) -> pd.DataFrame` that:
   - Reads the CSV
   - Renames `date` -> `game_date`, normalizes date format to match matchup features (YYYY-MM-DD string)
   - Maps `home` and `away` columns to 3-letter abbreviations using KAGGLE_TEAM_TO_ABB
   - Converts season format to match project convention (integer e.g. 200708)
   - Computes `covers_spread` from `id_spread` + `whos_favored`: home covered=1, away covered=0, push=NaN (see Research code example for compute_home_covers_spread)
   - Computes no-vig implied probabilities from moneyline columns: `home_implied_prob`, `away_implied_prob` using multiplicative normalization (raw_home / (raw_home + raw_away)). For rows with missing moneyline (ESPN-sourced post-Jan 2023), set implied_prob columns to NaN.
   - Returns DataFrame with columns: game_date, home_team, away_team, season, spread, home_implied_prob, away_implied_prob, covers_spread

4. Add `assert df.shape[0] > 0` after loading to catch empty file issues.

5. Use only ASCII characters in all print statements (Pitfall 7: Windows cp1252).

6. Make the module runnable as main: `if __name__ == "__main__"` prints row count, season range, and sample of unique team names for verification.
  </action>
  <verify>
    <automated>python -c "from src.data.get_historical_odds import load_and_normalize_odds; df = load_and_normalize_odds(); print(f'Rows: {df.shape[0]}, Cols: {list(df.columns)}'); assert df.shape[0] > 0; assert 'covers_spread' in df.columns; assert 'home_implied_prob' in df.columns; print('PASS')"</automated>
  </verify>
  <done>load_and_normalize_odds() returns a DataFrame with game_date, home_team, away_team, season, spread, home_implied_prob, away_implied_prob, covers_spread; team names are 3-letter abbreviations; pushes are NaN in covers_spread; no-vig implied probabilities are computed</done>
</task>

<task type="auto">
  <name>Task 2: Build ATS feature table by joining matchup features with odds</name>
  <files>src/features/ats_features.py, data/features/game_ats_features.csv</files>
  <action>
Create `src/features/ats_features.py` that:

1. Implement `build_ats_features()` function:

```python
def build_ats_features(
    matchup_path: str = "data/features/game_matchup_features.csv",
    odds_path: str = "data/raw/odds/nba_betting_historical.csv",
    output_path: str = "data/features/game_ats_features.csv",
) -> pd.DataFrame:
```

2. Load matchup features from `game_matchup_features.csv` and odds from `load_and_normalize_odds()`.

3. Join on `game_date + home_team + away_team` using inner join (only rows with known odds). This drops pre-2007 games as expected (Pitfall 6).

4. Add assertion: `assert merged.shape[0] > 0, "Zero rows after merge -- check team name mapping"` (Pitfall 2).

5. Add the spread column as `spread` (this is the opening line -- use as model input per Pitfall 5; closing line used only in CLV backtest).

6. Add `home_implied_prob` and `away_implied_prob` (no-vig) as columns. These are legitimate pre-game inputs.

7. Add `covers_spread` as the target column (1/0/NaN).

8. Exclude anomalous seasons matching EXCLUDED_SEASONS from game_outcome_model.py (201920, 202021).

9. Add a header docstring: "IMPORTANT: This file intentionally contains spread and implied probability. The win-probability model (game_outcome_model.py) MUST NOT use this file."

10. Save to `data/features/game_ats_features.csv`.

11. Print summary: total rows, rows with valid covers_spread (non-NaN), rows with valid implied probs, season range.

12. Verify data separation guard: assert that `game_matchup_features.csv` does NOT contain 'spread', 'covers_spread', 'home_implied_prob', or 'away_implied_prob' columns. This is the code-level FR-5.1 guard.

13. Make runnable as main: `if __name__ == "__main__"` calls `build_ats_features()` and prints summary.

14. ASCII only in print statements.
  </action>
  <verify>
    <automated>python -c "from src.features.ats_features import build_ats_features; df = build_ats_features(); print(f'ATS rows: {df.shape[0]}'); assert df.shape[0] > 500; assert 'spread' in df.columns; assert 'covers_spread' in df.columns; assert 'home_implied_prob' in df.columns; import pandas as pd; matchup = pd.read_csv('data/features/game_matchup_features.csv', nrows=1); assert 'spread' not in matchup.columns; assert 'covers_spread' not in matchup.columns; print('PASS: data separation verified')"</automated>
  </verify>
  <done>game_ats_features.csv exists with spread, home_implied_prob, away_implied_prob, covers_spread columns alongside all matchup features; game_matchup_features.csv confirmed to NOT contain spread/odds columns; inner join produces >500 rows; pushes excluded as NaN; anomalous seasons excluded</done>
</task>

</tasks>

<verification>
1. `data/features/game_ats_features.csv` exists and is separate from `game_matchup_features.csv`
2. ATS file contains `spread`, `home_implied_prob`, `away_implied_prob`, `covers_spread` columns
3. Matchup file does NOT contain any of these columns (data separation guard)
4. `covers_spread` has NaN for push games, 1 for home covers, 0 for away covers
5. No-vig implied probabilities sum to ~1.0 (not >1.0 vig-inclusive)
6. Inner join produces thousands of rows (not 0 -- team name mapping works)
</verification>

<success_criteria>
- `game_ats_features.csv` has >5000 rows with spread and implied probability columns
- `game_matchup_features.csv` remains unchanged (no spread data)
- `load_and_normalize_odds()` successfully maps all 30 team names
- Push rows have NaN in covers_spread (excluded from future training)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ats-model/05-01-SUMMARY.md`
</output>
