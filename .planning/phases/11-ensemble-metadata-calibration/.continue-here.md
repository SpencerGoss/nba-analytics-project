---
phase: 11-ensemble-metadata-calibration
task: 0
total_tasks: 3
status: complete
last_updated: "2026-03-05T05:51:37Z"
closes: [MODEL-03, MODEL-01, VALID-03]
depends_on: [phase-10 retrain complete]
---

<current_state>
Phase 11-01 completed 2026-03-05. See 11-01-SUMMARY.md.
get_strong_value_bets() added to value_bet_detector.py; calibration report path fixed;
metadata updated. All Phase 11 tasks complete.
</current_state>

<completed_work>
None — phase not yet started.
</completed_work>

<remaining_work>
- Task 1: Add get_strong_value_bets() to src/models/value_bet_detector.py (MODEL-03)
  - This function combines game outcome win probability + ATS edge signal
  - Returns ranked DataFrame of best value bets with score, edge, confidence
  - Should use both calibrated game outcome prob AND ATS model probability
  - Stacking approach: combine the two model scores into a single value_score
  - Example signature: get_strong_value_bets(min_edge=0.03, min_confidence=0.6) -> pd.DataFrame

- Task 2: Regenerate game_outcome_metadata.json (MODEL-01)
  - Currently stale: shows 68 features, pre-lineup trained_at date
  - Run: python src/models/run_evaluation.py (regenerates SHAP + metadata)
  - Or manually update the metadata JSON with correct feature count (73) and new accuracy
  - Verify game_outcome_metadata.json shows 73 features and current date

- Task 3: Fix calibration report path (VALID-03)
  - Currently: calibration artifacts at models/artifacts/calibration/
  - Required by spec: reports/calibration/
  - Fix: either copy/symlink, or update calibration.py output path to write to reports/calibration/
  - Then regenerate: python src/models/calibration.py
  - Verify reports/calibration/ exists with calibration_curve.png, calibration_metrics.csv
</remaining_work>

<decisions_made>
- get_strong_value_bets() should NOT require a stacking classifier (too heavy)
  Instead: weighted combination of game_outcome_edge + ats_edge with configurable weights
- Calibration path fix: update calibration.py to write to reports/calibration/ (simpler than symlink)
</decisions_made>

<blockers>
None — all tasks are pure code/script, no external dependencies.
</blockers>

<context>
Phase 11 is the smaller of the two gap-closure phases. The ensemble function is
conceptually simple (combine two model scores). The metadata and calibration fixes
are administrative cleanup. After Phase 11, invoke `superpowers:verification-before-completion` to verify
all 14 requirements are now satisfied, then `session-wrap-up` to close the milestone.
</context>

<next_action>
Read src/models/value_bet_detector.py to understand current structure,
then add get_strong_value_bets() function after the existing detect_value_bets().
</next_action>
